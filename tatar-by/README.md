# Необходимо создать файл .env.local в этой директории (не в /src)

# Содержимое файла:

```
NEXT_PUBLIC_JWT_SECRET=string
NEXT_PUBLIC_URL=string
COURSES_URL=string
```

# Команда для запуска:

`make build`
